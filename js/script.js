var fNum  = parseInt(prompt('Enter First Number'));
var sNum = parseInt(prompt('Enter Second Number'));

function add(fNum, sNum) {
  return fNum + sNum;
};

alert( 'The sum is' +' ' + add(fNum, sNum));